# Optional: set when not using the Vite dev proxy for /health
# VITE_API_URL=http://localhost:8080

# Override Vite proxy target (defaults to http://localhost:8080)
# VITE_API_PROXY_TARGET=http://localhost:8080
